<div class="side_bar">
  <div class="top_side_bar">
      <%= link_to current_user.name, edit_user_path(current_user), class: "user-name"%>
      <%= link_to 'チャットを作成する', new_room_path, class: "newroom-btn"%>
  </div>
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <a href="#"><%= room.name %></a>
      </div>
    </div>
  <% end %>
</div>